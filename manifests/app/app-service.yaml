apiVersion: v1
kind: Service
metadata:
  name: employee-management
  namespace: dev
  labels:
    app: employee-management
spec:
  selector:
    app: employee-management
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
